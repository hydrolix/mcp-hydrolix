*7 phases, 70 tasks; ends with release-pipeline rollout gate after sibling-repo and GAR provisioning land.*

## 1. Build hook + brand bake-in

- [x] 1.1 Create `hatch_build.py` with a `BuildHookInterface` subclass that reads `MCP_BRAND={hydrolix|trafficpeak}` (default `hydrolix`) and rejects any other value with a descriptive error [implements: design/hatchling-hook] — verify: `MCP_BRAND=foo uv build` fails with a clear error naming acceptable values
- [x] 1.2 In the hook, mutate `project.name`, `[project.scripts]` entry key, `project.urls`, `project.description`, and author/maintainer metadata per a per-brand table [implements: mcp-server-branding/two-pypi-distributions-built-from-one-source-via-a-build-time-brand-flag] — verify: `MCP_BRAND=trafficpeak uv build` produces a wheel whose `METADATA` declares `Name: mcp-trafficpeak`
- [x] 1.3 In the hook, write `mcp_hydrolix/_brand.py` with `__brand__` (short identifier, e.g. `"trafficpeak"`) and `__dist_name__` (matching PyPI distribution name, e.g. `"mcp-trafficpeak"`) constants set from the per-brand vars table, included in the wheel's package contents [implements: mcp-server-branding/runtime-brand-identifier-baked-at-build-time] — verify: `unzip -p dist/mcp_trafficpeak-*.whl mcp_hydrolix/_brand.py` prints both `__brand__ = "trafficpeak"` and `__dist_name__ = "mcp-trafficpeak"`
- [x] 1.3a In the hook, read this repo's `README.md`, apply a case-insensitive, case-preserving brand-substitution filter (every `hydrolix` occurrence → target brand: `mcp-hydrolix`, `HYDROLIX_`, `Hydrolix`, `hydrolix.live`, `mcp--hydrolix` badges, lowercase `hydrolix_*` keys all rebrand), and inject the result as the wheel's `long_description`. Hydrolix mode is an identity transform; TrafficPeak-mode prose contains zero `hydrolix` except the exempt GitHub-org identity namespace `io.github.hydrolix`/`github.com/hydrolix`. [implements: mcp-server-branding/brand-appropriate-distribution-metadata-with-zero-cross-brand-leakage] — verify: scenario tests "Long Description Is Generated By The Brand Filter From This Repos Readme" and "Long Description Tokens Are Rebranded For Trafficpeak Builds" pass
- [x] 1.4 Add hook reference to `pyproject.toml` under `[tool.hatch.build.hooks.custom]` (or equivalent), keeping `[project]` block as the Hydrolix-default values [implements: design/hatchling-hook] — verify: `uv build` with no env var still produces today's `mcp-hydrolix` wheel
- [x] 1.5 Replace any runtime brand-detection code paths (notably anything inspecting `sys.argv[0]` for brand) with `from mcp_hydrolix._brand import __brand__, __dist_name__` [implements: design/brand-identifier-baked] — verify: `grep -rn 'argv\[0\]' mcp_hydrolix/` returns no brand-detection hit
- [x] 1.6 Replace the hardcoded `MCP_SERVER_NAME = "mcp-hydrolix"` literal in `mcp_hydrolix/mcp_server.py` with `MCP_SERVER_NAME = __dist_name__`; the FastMCP server constructor's `name=` argument and the `User` token of `hdx_query_admin_comment` flow through this constant unchanged [implements: mcp-server-branding/runtime-brand-identifier-baked-at-build-time] — verify: `grep -n '"mcp-hydrolix"' mcp_hydrolix/` returns no matches; scenarios "Fastmcp Server Name Reflects The Baked Distribution Name" and "Admin Comment User Token Reflects The Baked Distribution Name" pass
- [x] 1.6a Use `__dist_name__` to construct the startup log line and the leading token of the outbound HTTP `User-Agent` header [implements: mcp-server-branding/runtime-brand-identifier-baked-at-build-time] — verify: scenarios "Brand Identifier In Startup Log Reflects Baked Constant" and "Brand Identifier In Outbound User Agent Reflects Baked Constant" pass
- [x] 1.7 Document in `hatch_build.py` docstring that the canonical way to inspect either wheel's metadata is `unzip -p dist/*.whl '*/METADATA'` after a build [implements: meta/docs] — verify: docstring exists and is referenced from CONTRIBUTING (if present) or top of `hatch_build.py`

## 2. Dual-namespace runtime env resolution

- [x] 2.1 Introduce `brand_getenv(name)`, a read-time coalesce returning the `TRAFFICPEAK_<suffix>` value when set else the `HYDROLIX_<suffix>` value, with no `os.environ` mutation; deprecated-alias suffixes (`HOST`/`PORT`/`SECURE`/`API_HOST`/`API_PORT`/`PROXY_PATH`) are Hydrolix-only and never mirrored [implements: design/dual-namespace-precedence] — verify: unit tests show a `TRAFFICPEAK_*` value is preferred, a `TRAFFICPEAK_HOST` is NOT mirrored, and `os.environ` is unchanged after config construction
- [x] 2.2 At server startup, resolve per-variable at read time: each read of a canonical `HYDROLIX_<suffix>` goes through `brand_getenv`, which prefers the set `TRAFFICPEAK_<suffix>` [implements: mcp-server-branding/dual-namespace-env-var-contract-with-per-variable-precedence] — verify: scenario tests for "Only Trafficpeak Env Vars Provided" and "Trafficpeak Overrides Apply Per Variable" pass
- [x] 2.3 Where a `TRAFFICPEAK_<suffix>` is unset, the existing `HYDROLIX_<suffix>` is used unchanged (silent fallback); no `HYDROLIX_*` var is deleted [implements: mcp-server-branding/dual-namespace-env-var-contract-with-per-variable-precedence] — verify: scenario test "Only Hydrolix Env Vars Provided" passes
- [x] 2.4 When both a `TRAFFICPEAK_<suffix>` and its `HYDROLIX_<suffix>` are set, TrafficPeak wins silently for that variable — no cross-namespace conflict warning is emitted [implements: mcp-server-branding/dual-namespace-env-var-contract-with-per-variable-precedence] — verify: scenario tests "Both Namespaces Resolve With Conflicting Anchors" and "Both Namespaces Resolve To Identical Values" pass
- [x] 2.5 If neither namespace yields a URL, exit non-zero with an error naming only the running brand's own anchor (`TRAFFICPEAK_URL` for a TP build, `HYDROLIX_URL` for a Hydrolix build) [implements: mcp-server-branding/dual-namespace-env-var-contract-with-per-variable-precedence] — verify: scenario test "Neither Namespace Provides An Anchor" passes

## 3. mcpb bundle parameterization

- [x] 3.1 Add a per-brand vars table (shell associative array or sourced file) to `mcpb/build.sh` and read `MCP_BRAND` (default `hydrolix`) [implements: design/mcpb-template-parameterized] — verify: `MCP_BRAND=trafficpeak bash mcpb/build.sh` exits 0 and produces a `dist/*trafficpeak*.mcpb`
- [x] 3.2 Replace literal Hydrolix strings in `mcpb/manifest.json.tmpl` (`name`, `display_name`, `description`, `long_description`, `author`, `homepage`, `repository`, `keywords`, `user_config` field keys/titles, `server.mcp_config.env` keys) with placeholders substituted by `build.sh` from the vars table [implements: mcp-server-branding/mcpb-bundle-is-brand-parameterized] — verify: `MCP_BRAND=trafficpeak bash mcpb/build.sh && unzip -p dist/mcp-trafficpeak-*.mcpb manifest.json | grep -i hydrolix` returns no matches
- [x] 3.3 Update `mcpb/pyproject.toml.tmpl` to declare brand-matching dist name in the bundle's embedded venv [implements: design/mcpb-template-parameterized] — verify: `MCP_BRAND=trafficpeak bash mcpb/build.sh` produces a bundle whose embedded `pyproject.toml` declares `mcp-trafficpeak` (not `mcp-hydrolix`) as the installed distribution
- [x] 3.4 Bundle output filename convention: `dist/mcp-<brand>-<version>.mcpb` [implements: design/mcpb-template-parameterized] — verify: bundles for both brands at the same version coexist in `dist/` without name collision

## 4. Release pipeline in this repo

- [x] 4.1 In `.github/workflows/publish.yml` `publish` job, build and publish ONLY `mcp-hydrolix` to PyPI (single `uv build` with `MCP_BRAND=hydrolix`, single `pypi-publish` step) [implements: mcp-server-branding/two-pypi-distributions-built-from-one-source-via-a-build-time-brand-flag] — verify: a release run uploads `mcp-hydrolix==<v>` to PyPI from this repo's workflow (and ONLY `mcp-hydrolix`)
- [x] 4.1a Add a dispatch step to `publish.yml` that fires `gh workflow run publish.yml -R hydrolix/mcp-trafficpeak -f tag=$TAG` after the Hydrolix wheel is uploaded; the step MUST exit non-zero if the dispatch API call fails [implements: design/tp-pypi-via-sibling-repo] — verify: scenario test "Sibling Repo Dispatch Failure Fails The Release" passes (a synthetic unreachable sibling causes the release to fail)
- [x] 4.2 In `publish.yml` `publish-mcpb` job, invoke `mcpb/build.sh` twice (`MCP_BRAND=hydrolix` then `MCP_BRAND=trafficpeak`), attach both `.mcpb` files to the GitHub Release [implements: mcp-server-branding/artifact-parity-invariant] — verify: a release run produces both `dist/mcp-hydrolix-<v>.mcpb` and `dist/mcp-trafficpeak-<v>.mcpb` and both are attached to the release
- [x] 4.3 In `publish.yml` `publish-docker` job, pass `MCP_BRAND` as a build-arg to `docker buildx build` and run the build+push pair twice with brand-substituted image names (`mcp-hydrolix` and `mcp-trafficpeak`) [implements: mcp-server-branding/artifact-parity-invariant] — verify: a release run pushes both `us-docker.pkg.dev/hdx-art/t/mcp-hydrolix:v<v>` and `us-docker.pkg.dev/hdx-art/t/mcp-trafficpeak:v<v>` (and `:latest`)
- [x] 4.4 Adjust `Dockerfile` to consume `MCP_BRAND` build-arg and run `MCP_BRAND=$MCP_BRAND uv build` inside the image so the embedded wheel matches the brand [implements: design/artifact-parity] — verify: `docker run mcp-trafficpeak:test mcp-trafficpeak --version`-equivalent (or `pip show mcp-trafficpeak` inside the image) reports the TP brand
- [x] 4.5 Add a parity-assertion step at the end of each in-repo artifact-producing job that fails the workflow if either brand counterpart is missing (mcpb: both `.mcpb` files in `dist/`; docker: both tags pushed). Skip PyPI in this assertion — TP PyPI verification is post-release (task 7.5) because it's async. [implements: design/artifact-parity] — verify: scenario test "Ci Fails If Pairing Is Broken" passes (a synthetic missing-brand setup fails the assertion)
- [x] 4.6 Provision a GitHub App or PAT secret in this repo with `actions: write` on `hydrolix/mcp-trafficpeak`; reference it from the dispatch step's `gh workflow run` invocation [implements: design/tp-pypi-via-sibling-repo] — verify: `gh workflow run publish.yml -R hydrolix/mcp-trafficpeak -f tag=v0.0.0-dryrun` from CI's environment succeeds (or fails with a non-permission error)
- [x] 4.7 Update `publish.yml`'s `bump-version` job `needs:` list so the post-release version bump still gates on all in-repo artifact jobs (not on the async sibling completion) [implements: meta/tooling] — verify: workflow YAML parses; `needs:` includes `publish`, `publish-docker`, `publish-mcpb`

## 5. Sibling repo bootstrap

- [x] 5.1 Create `hydrolix/mcp-trafficpeak` repo (empty default branch) [implements: meta/rollout] — verify: `gh repo view hydrolix/mcp-trafficpeak` succeeds
- [x] 5.2 Add the sibling's publish workflow at `.github/workflows/publish.yml` declaring `on: workflow_dispatch:` with a required `tag: string` input; the workflow checks out `hydrolix/mcp-hydrolix@${{ inputs.tag }}`, runs `MCP_BRAND=trafficpeak uv build` in the checkout directory, then publishes via Trusted Publisher [implements: mcp-server-branding/only-pypi-publishing-goes-through-the-sibling-repo, design/tp-pypi-via-sibling-repo] — verify: scenarios "Tp Wheel Is Built From This Repos Tagged Commit", "Sibling Repo Publish Workflow Uses Workflow Dispatch", "Release Tag Publishes Both Pypi Distributions At The Same Version", and "Tp Pypi Landing Page Is Generated From This Repos Readme By The Brand Filter" all pass
- [x] 5.3 Add the sibling's `README.md` (short — describes why the repo exists and how the `workflow_dispatch` flow from `hydrolix/mcp-hydrolix` works; for GitHub visitors only; MUST NOT contain customer install instructions, which reach PyPI via the brand filter applied to this repo's `README.md`) [implements: mcp-server-branding/only-pypi-publishing-goes-through-the-sibling-repo] — verify: `grep -iE 'install|uvx|uv run|pip install' README.md` in sibling repo returns no matches; sibling repo contains no `PYPI_README.md` (`test ! -f PYPI_README.md`)
- [x] 5.4 Add an issue-template under `.github/ISSUE_TEMPLATE/` that triages between "install/packaging issues for mcp-trafficpeak — file here" and "server behavior / feature requests — file on hydrolix/mcp-hydrolix" [implements: design/tp-pypi-via-sibling-repo] — verify: the template renders on the sibling repo's new-issue page
- [ ] 5.5 File the PyPI Trusted Publisher request for project `mcp-trafficpeak` bound to `hydrolix/mcp-trafficpeak` / `publish.yml` / `pypi` environment [implements: meta/rollout] — verify: `mcp-trafficpeak` PyPI page lists the sibling repo as its Trusted Publisher
- [x] 5.6 Verify the sibling contains nothing beyond the workflow + README + issue template [implements: mcp-server-branding/only-pypi-publishing-goes-through-the-sibling-repo] — verify: scenario "Sibling Repo Contains No Source Or Tests" passes

## 6. Tests (one task per scenario)

- [x] 6.1 Add test for scenario `Default Build Produces The Hydrolix Wheel` [implements: mcp-server-branding/two-pypi-distributions-built-from-one-source-via-a-build-time-brand-flag, meta/tests] — verify: `pytest -q tests/test_mcp_server_branding.py::test_default_build_produces_the_hydrolix_wheel` green
- [x] 6.2 Add test for scenario `Trafficpeak Build Via Env Var Produces The Tp Wheel` [implements: mcp-server-branding/two-pypi-distributions-built-from-one-source-via-a-build-time-brand-flag, meta/tests] — verify: `pytest -q tests/test_mcp_server_branding.py::test_trafficpeak_build_via_env_var_produces_the_tp_wheel` green
- [x] 6.3 Add test for scenario `Invalid Mcp Brand Value Fails Fast` [implements: mcp-server-branding/two-pypi-distributions-built-from-one-source-via-a-build-time-brand-flag, meta/tests] — verify: `pytest -q tests/test_mcp_server_branding.py::test_invalid_mcp_brand_value_fails_fast` green
- [ ] 6.4 Add test for scenario `Tp Wheel Is Built From This Repos Tagged Commit` [implements: mcp-server-branding/two-pypi-distributions-built-from-one-source-via-a-build-time-brand-flag, meta/tests] — verify: `pytest -q tests/test_mcp_server_branding.py::test_tp_wheel_is_built_from_this_repos_tagged_commit` green
- [x] 6.5 Add test for scenario `Pip Show On Tp Wheel Has No Hydrolix String` [implements: mcp-server-branding/brand-appropriate-distribution-metadata-with-zero-cross-brand-leakage, meta/tests] — verify: `pytest -q tests/test_mcp_server_branding.py::test_pip_show_on_tp_wheel_has_no_hydrolix_string` green
- [x] 6.6 Add test for scenario `Pip Show On Hydrolix Wheel Is Unchanged` [implements: mcp-server-branding/brand-appropriate-distribution-metadata-with-zero-cross-brand-leakage, meta/tests] — verify: `pytest -q tests/test_mcp_server_branding.py::test_pip_show_on_hydrolix_wheel_is_unchanged` green
- [ ] 6.7 Add test for scenario `Uv Tree Shows Only The Installed Brand` [implements: mcp-server-branding/brand-appropriate-distribution-metadata-with-zero-cross-brand-leakage, meta/tests] — verify: `pytest -q tests/test_mcp_server_branding.py::test_uv_tree_shows_only_the_installed_brand` green
- [x] 6.8 Add test for scenario `Long Description Is Generated By The Brand Filter From This Repos Readme` [implements: mcp-server-branding/brand-appropriate-distribution-metadata-with-zero-cross-brand-leakage, meta/tests] — verify: `pytest -q tests/test_mcp_server_branding.py::test_long_description_is_generated_by_the_brand_filter_from_this_repos_readme` green
- [x] 6.8a Add test for scenario `Long Description Tokens Are Rebranded For Trafficpeak Builds` [implements: mcp-server-branding/brand-appropriate-distribution-metadata-with-zero-cross-brand-leakage, meta/tests] — verify: `pytest -q tests/test_mcp_server_branding.py::test_long_description_tokens_are_rebranded_for_trafficpeak_builds` green
- [x] 6.9 Add test for scenario `Brand Identifier In Startup Log Reflects Baked Constant` [implements: mcp-server-branding/runtime-brand-identifier-baked-at-build-time, meta/tests] — verify: `pytest -q tests/test_mcp_server_branding.py::test_brand_identifier_in_startup_log_reflects_baked_constant` green
- [x] 6.10 Add test for scenario `Brand Identifier In Outbound User Agent Reflects Baked Constant` [implements: mcp-server-branding/runtime-brand-identifier-baked-at-build-time, meta/tests] — verify: `pytest -q tests/test_mcp_server_branding.py::test_brand_identifier_in_outbound_user_agent_reflects_baked_constant` green
- [x] 6.11 Add test for scenario `Renaming The Launcher Does Not Change The Reported Brand` [implements: mcp-server-branding/runtime-brand-identifier-baked-at-build-time, meta/tests] — verify: `pytest -q tests/test_mcp_server_branding.py::test_renaming_the_launcher_does_not_change_the_reported_brand` green
- [x] 6.11a Add test for scenario `Fastmcp Server Name Reflects The Baked Distribution Name` [implements: mcp-server-branding/runtime-brand-identifier-baked-at-build-time, meta/tests] — verify: `pytest -q tests/test_mcp_server_branding.py::test_fastmcp_server_name_reflects_the_baked_distribution_name` green
- [x] 6.11b Add test for scenario `Admin Comment User Token Reflects The Baked Distribution Name` [implements: mcp-server-branding/runtime-brand-identifier-baked-at-build-time, meta/tests] — verify: `pytest -q tests/test_mcp_server_branding.py::test_admin_comment_user_token_reflects_the_baked_distribution_name` green
- [x] 6.11c Add test for scenario `No Customer-Visible Output Of A Trafficpeak Wheel Contains The Hydrolix Distribution Name` [implements: mcp-server-branding/runtime-brand-identifier-baked-at-build-time, meta/tests] — verify: `pytest -q tests/test_mcp_server_branding.py::test_no_customer_visible_output_of_a_trafficpeak_wheel_contains_the_hydrolix_distribution_name` green
- [x] 6.12 Add test for scenario `Only Trafficpeak Env Vars Provided` [implements: mcp-server-branding/dual-namespace-env-var-contract-with-per-variable-precedence, meta/tests] — verify: `pytest -q tests/test_mcp_server_branding.py::test_only_trafficpeak_env_vars_provided` green
- [x] 6.13 Add test for scenario `Only Hydrolix Env Vars Provided` [implements: mcp-server-branding/dual-namespace-env-var-contract-with-per-variable-precedence, meta/tests] — verify: `pytest -q tests/test_mcp_server_branding.py::test_only_hydrolix_env_vars_provided` green
- [x] 6.14 Add test for scenario `Trafficpeak Overrides Apply Per Variable` [implements: mcp-server-branding/dual-namespace-env-var-contract-with-per-variable-precedence, meta/tests] — verify: `pytest -q tests/test_mcp_server_branding.py::test_trafficpeak_overrides_apply_per_variable_over_hydrolix_fallback` green
- [x] 6.15 Add test for scenario `Both Namespaces Resolve With Conflicting Anchors` [implements: mcp-server-branding/dual-namespace-env-var-contract-with-per-variable-precedence, meta/tests] — verify: `pytest -q tests/test_mcp_server_branding.py::test_both_namespaces_resolve_with_conflicting_anchors` green
- [x] 6.16 Add test for scenario `Both Namespaces Resolve To Identical Values` [implements: mcp-server-branding/dual-namespace-env-var-contract-with-per-variable-precedence, meta/tests] — verify: `pytest -q tests/test_mcp_server_branding.py::test_both_namespaces_resolve_to_identical_values` green
- [x] 6.17 Add test for scenario `Neither Namespace Provides An Anchor` [implements: mcp-server-branding/dual-namespace-env-var-contract-with-per-variable-precedence, meta/tests] — verify: `pytest -q tests/test_mcp_server_branding.py::test_neither_namespace_provides_an_anchor` green
- [x] 6.18 Add test for scenario `Pre-Change Customer Config Still Works` [implements: mcp-server-branding/existing-hydrolix-branded-surface-is-preserved, meta/tests] — verify: `pytest -q tests/test_mcp_server_branding.py::test_pre_change_customer_config_still_works` green
- [x] 6.19 Add test for scenario `Python Import Path Is Unchanged` [implements: mcp-server-branding/existing-hydrolix-branded-surface-is-preserved, meta/tests] — verify: `pytest -q tests/test_mcp_server_branding.py::test_python_import_path_is_unchanged` green
- [x] 6.20 Add test for scenario `Readme Does Not Mention Trafficpeak` [implements: mcp-server-branding/this-repositorys-customer-facing-documentation-references-only-the-hydrolix-brand, meta/tests] — verify: `pytest -q tests/test_mcp_server_branding.py::test_readme_does_not_mention_trafficpeak` green
- [x] 6.21 Add test for scenario `Internal Engineering Docs May Mention Both Brands` [implements: mcp-server-branding/this-repositorys-customer-facing-documentation-references-only-the-hydrolix-brand, meta/tests] — verify: `pytest -q tests/test_mcp_server_branding.py::test_internal_engineering_docs_may_mention_both_brands` green
- [ ] 6.22 Add test for scenario `Release Tag Publishes Both Pypi Distributions At The Same Version` [implements: mcp-server-branding/artifact-parity-invariant, meta/tests] — verify: `pytest -q tests/test_mcp_server_branding.py::test_release_tag_publishes_both_pypi_distributions_at_the_same_version` green
- [ ] 6.23 Add test for scenario `Sibling Repo Dispatch Failure Fails The Release` [implements: mcp-server-branding/artifact-parity-invariant, meta/tests] — verify: `pytest -q tests/test_mcp_server_branding.py::test_sibling_repo_dispatch_failure_fails_the_release` green
- [ ] 6.24 Add test for scenario `Release Tag Publishes Both Mcpb Bundles At The Same Version` [implements: mcp-server-branding/artifact-parity-invariant, meta/tests] — verify: `pytest -q tests/test_mcp_server_branding.py::test_release_tag_publishes_both_mcpb_bundles_at_the_same_version` green
- [ ] 6.25 Add test for scenario `Release Tag Publishes Both Docker Images At The Same Version` [implements: mcp-server-branding/artifact-parity-invariant, meta/tests] — verify: `pytest -q tests/test_mcp_server_branding.py::test_release_tag_publishes_both_docker_images_at_the_same_version` green
- [ ] 6.26 Add test for scenario `Ci Fails If Pairing Is Broken` [implements: mcp-server-branding/artifact-parity-invariant, meta/tests] — verify: `pytest -q tests/test_mcp_server_branding.py::test_ci_fails_if_pairing_is_broken` green
- [x] 6.27 Add test for scenario `Tp Mode Mcpb Build Emits A Tp-Branded Bundle` [implements: mcp-server-branding/mcpb-bundle-is-brand-parameterized, meta/tests] — verify: `pytest -q tests/test_mcp_server_branding.py::test_tp_mode_mcpb_build_emits_a_tp_branded_bundle` green
- [x] 6.28 Add test for scenario `Default Mode Mcpb Build Emits The Hydrolix Bundle Unchanged` [implements: mcp-server-branding/mcpb-bundle-is-brand-parameterized, meta/tests] — verify: `pytest -q tests/test_mcp_server_branding.py::test_default_mode_mcpb_build_emits_the_hydrolix_bundle_unchanged` green
- [ ] 6.29 Add test for scenario `Sibling Repo Contains No Source Or Tests` [implements: mcp-server-branding/only-pypi-publishing-goes-through-the-sibling-repo, meta/tests] — verify: `pytest -q tests/test_mcp_server_branding.py::test_sibling_repo_contains_no_source_or_tests` green
- [ ] 6.30 Add test for scenario `Sibling Repo Publish Workflow Uses Workflow Dispatch` [implements: mcp-server-branding/only-pypi-publishing-goes-through-the-sibling-repo, meta/tests] — verify: `pytest -q tests/test_mcp_server_branding.py::test_sibling_repo_publish_workflow_uses_workflow_dispatch` green
- [ ] 6.31 Add test for scenario `Tp Pypi Landing Page Is Generated From This Repos Readme By The Brand Filter` [implements: mcp-server-branding/only-pypi-publishing-goes-through-the-sibling-repo, meta/tests] — verify: `pytest -q tests/test_mcp_server_branding.py::test_tp_pypi_landing_page_is_generated_from_this_repos_readme_by_the_brand_filter` green

## 7. Docs + rollout gate

- [x] 7.1 Confirm `README.md` and other customer-facing doc files in this repo do not reference TrafficPeak [implements: mcp-server-branding/this-repositorys-customer-facing-documentation-references-only-the-hydrolix-brand] — verify: `grep -irE 'trafficpeak' README.md docker-compose.yaml glama.json fastmcp.json` returns no matches
- [ ] 7.2 Verify the `mcp-trafficpeak` GAR repository (`us-docker.pkg.dev/hdx-art/t/mcp-trafficpeak`) exists and the existing CI service account has push permission [implements: meta/rollout] — verify: `gcloud artifacts repositories describe mcp-trafficpeak --location=us` returns 0 (or equivalent existence check)
- [ ] 7.3 Cut a release tag and confirm all six artifacts publish atomically across both repos (2 PyPI dists, 2 mcpb bundles, 2 Docker images) [implements: meta/rollout] — verify: post-release, `pip show mcp-hydrolix==<v>`, `pip show mcp-trafficpeak==<v>`, both GAR digests, and both `.mcpb` files attached to the GitHub Release at tag `v<v>` all exist, and the sibling repo's workflow shows a successful run for the matching tag
