*4 phases, 22 tasks; new module, config loader, startup preflight, 14 scenario tests, and docs.*

## 1. IdP Endpoint Module

- [ ] 1.1 Create `mcp_hydrolix/auth/idp_endpoints.py`: define `CanonicalIdPEndpoints` as `@dataclass(frozen=True)` with fields `issuer: str`, `discovery_url: str`, `jwks_uri: str`, `address: str` [implements: design/single-idp-coupling-point, oauth-config-and-preflight/canonical-idp-endpoint-derivation-is-a-single-contained-function] — verify: `ruff check mcp_hydrolix/auth/idp_endpoints.py` clean and module imports without error
- [ ] 1.2 Add `canonical_idp_endpoints(hydrolix_url: str) -> CanonicalIdPEndpoints` stub that raises `NotImplementedError` with a message containing the literal string `HDX-11431` [implements: design/single-idp-coupling-point, oauth-config-and-preflight/canonical-idp-endpoint-derivation-is-a-single-contained-function] — verify: calling the function in a Python REPL raises `NotImplementedError` with `HDX-11431` in the message

## 2. Config Loader

- [ ] 2.1 Add `OAuthConfigError` exception class to `mcp_hydrolix/auth/oauth.py` if not already present [implements: design/two-distinct-fatal-startup-exceptions, oauth-config-and-preflight/activation-gated-on-env-vars] — verify: `from mcp_hydrolix.auth.oauth import OAuthConfigError` succeeds
- [ ] 2.2 Add or update `OAuthConfig` frozen dataclass in `mcp_hydrolix/auth/oauth.py` with fields: `issuer: str`, `audience: list[str]`, `jwks_uri: str | None`, `allow_insecure_jwks: bool`, `required_scopes: list[str]`, `resource_url: str | None` [implements: oauth-config-and-preflight/activation-gated-on-env-vars, oauth-config-and-preflight/jwks-uri-override-and-insecure-transport-flag] — verify: `ruff check mcp_hydrolix/auth/oauth.py` clean
- [ ] 2.3 Implement `load_oauth_config()` in `mcp_hydrolix/auth/oauth.py`: read all `HYDROLIX_OAUTH_*` env vars; apply issuer precedence (explicit `HYDROLIX_OAUTH_ISSUER` > `canonical_idp_endpoints(HYDROLIX_URL).issuer` > unset); raise `OAuthConfigError` for all partial-configuration cases; let `NotImplementedError` from `canonical_idp_endpoints` propagate unwrapped [implements: design/two-distinct-fatal-startup-exceptions, design/single-idp-coupling-point, oauth-config-and-preflight/activation-gated-on-env-vars] — verify: `ruff check mcp_hydrolix/auth/oauth.py` clean and unit smoke test passes
- [ ] 2.4 Add insecure-JWKS-URI guard in `load_oauth_config()`: when `HYDROLIX_OAUTH_JWKS_URI` is set with `http://` scheme and `HYDROLIX_OAUTH_ALLOW_INSECURE_JWKS` is not `"true"`, raise `OAuthConfigError` before any network call [implements: oauth-config-and-preflight/jwks-uri-override-and-insecure-transport-flag] — verify: unit test for `plain_http_jwks_rejected_by_default` scenario passes

## 3. Startup Preflight Wiring

- [ ] 3.1 Add `_activate_oauth_if_configured()` helper in `mcp_hydrolix/webapp.py`: call `load_oauth_config()`; if it returns `None` (no audience set), return immediately; otherwise run async OIDC discovery + JWKS preflight via `asyncio.run(try_activate_oauth(cfg))`; on network/HTTP/JSON errors, log exactly one WARNING containing `OAuth configured but not activated` and the exception class name, and return without setting `mcp.auth` [implements: oauth-config-and-preflight/startup-preflight-is-fail-open, design/two-distinct-fatal-startup-exceptions] — verify: `ruff check mcp_hydrolix/webapp.py` clean
- [ ] 3.2 Call `_activate_oauth_if_configured()` from `create_app()` before `mcp.http_app(...)` [implements: oauth-config-and-preflight/startup-preflight-is-fail-open] — verify: `python -c "from mcp_hydrolix.webapp import create_app"` exits 0 with no OAuth vars set (byte-identical path)
- [ ] 3.3 Confirm no code outside `mcp_hydrolix/auth/idp_endpoints.py` encodes IdP URL structure relative to `HYDROLIX_URL` [implements: design/single-idp-coupling-point] — verify: `grep -r "HYDROLIX_URL" mcp_hydrolix/auth/ --include="*.py" -l` lists only `idp_endpoints.py` and `oauth.py` (the latter only for reading the env var, not URL construction)

## 4. Tests

- [ ] 4.1 Add test for scenario "No Oauth Vars Set" [implements: oauth-config-and-preflight/activation-gated-on-env-vars, meta/tests] — verify: `pytest -q tests/auth/test_oauth_config_and_preflight.py::test_no_oauth_vars_set` green
- [ ] 4.2 Add test for scenario "Audience Set But No Issuer Resolvable" [implements: oauth-config-and-preflight/activation-gated-on-env-vars, meta/tests] — verify: `pytest -q tests/auth/test_oauth_config_and_preflight.py::test_audience_set_but_no_issuer_resolvable` green
- [ ] 4.3 Add test for scenario "Issuer Derivation Attempted Before Hdx-11431" [implements: oauth-config-and-preflight/activation-gated-on-env-vars, meta/tests] — verify: `pytest -q tests/auth/test_oauth_config_and_preflight.py::test_issuer_derivation_attempted_before_hdx_11431` green
- [ ] 4.4 Add test for scenario "Issuer Derived From Cluster Url Post-Hdx-11431" (marked `xfail` until [HDX-11431](https://hydrolix.atlassian.net/browse/HDX-11431) lands) [implements: oauth-config-and-preflight/activation-gated-on-env-vars, meta/tests] — verify: `pytest -q tests/auth/test_oauth_config_and_preflight.py::test_issuer_derived_from_cluster_url_post_hdx_11431` xfail (not error)
- [ ] 4.5 Add test for scenario "Explicit Issuer Overrides Derivation" [implements: oauth-config-and-preflight/activation-gated-on-env-vars, meta/tests] — verify: `pytest -q tests/auth/test_oauth_config_and_preflight.py::test_explicit_issuer_overrides_derivation` green
- [ ] 4.6 Add test for scenario "Issuer Set But Audience Unset" [implements: oauth-config-and-preflight/activation-gated-on-env-vars, meta/tests] — verify: `pytest -q tests/auth/test_oauth_config_and_preflight.py::test_issuer_set_but_audience_unset` green
- [ ] 4.7 Add test for scenario "Hydrolix Url Set Without Oauth Vars" [implements: oauth-config-and-preflight/activation-gated-on-env-vars, meta/tests] — verify: `pytest -q tests/auth/test_oauth_config_and_preflight.py::test_hydrolix_url_set_without_oauth_vars` green
- [ ] 4.8 Add test for scenario "Optional Oauth Var Set Without Audience" [implements: oauth-config-and-preflight/activation-gated-on-env-vars, meta/tests] — verify: `pytest -q tests/auth/test_oauth_config_and_preflight.py::test_optional_oauth_var_set_without_audience` green
- [ ] 4.9 Add test for scenario "Stub Raises Not Implemented Error Until Hdx-11431" [implements: oauth-config-and-preflight/canonical-idp-endpoint-derivation-is-a-single-contained-function, meta/tests] — verify: `pytest -q tests/auth/test_oauth_config_and_preflight.py::test_stub_raises_not_implemented_error_until_hdx_11431` green
- [ ] 4.10 Add test for scenario "Eventual Return Shape Is Immutable And Complete" (marked `xfail` until [HDX-11431](https://hydrolix.atlassian.net/browse/HDX-11431) lands) [implements: oauth-config-and-preflight/canonical-idp-endpoint-derivation-is-a-single-contained-function, meta/tests] — verify: `pytest -q tests/auth/test_oauth_config_and_preflight.py::test_eventual_return_shape_is_immutable_and_complete` xfail (not error)
- [ ] 4.11 Add test for scenario "Eventual Derived Issuer Is Never Equal To Input Cluster Url" (marked `xfail` until [HDX-11431](https://hydrolix.atlassian.net/browse/HDX-11431) lands) [implements: oauth-config-and-preflight/canonical-idp-endpoint-derivation-is-a-single-contained-function, meta/tests] — verify: `pytest -q tests/auth/test_oauth_config_and_preflight.py::test_eventual_derived_issuer_is_never_equal_to_input_cluster_url` xfail (not error)
- [ ] 4.12 Add test for scenario "Plain Http Jwks Rejected By Default" [implements: oauth-config-and-preflight/jwks-uri-override-and-insecure-transport-flag, meta/tests] — verify: `pytest -q tests/auth/test_oauth_config_and_preflight.py::test_plain_http_jwks_rejected_by_default` green
- [ ] 4.13 Add test for scenario "Plain Http Jwks Allowed When Explicitly Opted In" [implements: oauth-config-and-preflight/jwks-uri-override-and-insecure-transport-flag, meta/tests] — verify: `pytest -q tests/auth/test_oauth_config_and_preflight.py::test_plain_http_jwks_allowed_when_explicitly_opted_in` green
- [ ] 4.14 Add test for scenario "Discovery Network Failure At Startup" [implements: oauth-config-and-preflight/startup-preflight-is-fail-open, meta/tests] — verify: `pytest -q tests/auth/test_oauth_config_and_preflight.py::test_discovery_network_failure_at_startup` green

## 5. Documentation

- [ ] 5.1 Update or create `docs/oauth.md`: document all `HYDROLIX_OAUTH_*` env vars, the issuer precedence chain, the interim requirement for explicit `HYDROLIX_OAUTH_ISSUER` until [HDX-11431](https://hydrolix.atlassian.net/browse/HDX-11431) lands, OIDC discovery latency at worker startup, and the insecure-JWKS opt-in [implements: meta/docs] — verify: `docs/oauth.md` exists and contains the substring `HYDROLIX_OAUTH_ISSUER` and the substring `HDX-11431`
