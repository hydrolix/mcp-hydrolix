*Resolved 0 decisions; 0 assumptions; 0 items deferred.*

## Questions Asked

*none — see No Ambiguity below*

## No Ambiguity

This sub-spec is extracted directly from the reviewed and approved `oauth-prototype-productionize` parent spec (requirements R08 and R09). Every behavior — the endpoint path, the JSON fields, the three-tier resource URL precedence chain, the partial-config error path, and the `WWW-Authenticate` `resource_metadata=` parameter — is fully specified in the parent. No design choices remain open: the parent spec was operator-confirmed as part of the [HDX-11442](https://hydrolix.atlassian.net/browse/HDX-11442) OAuth productionization effort. This change is a mechanical extraction; no new trade-offs need adjudication here.

## Deferred / Out of Scope

*none*

## Assumptions

*none*
