*Resolved 0 decisions; 0 assumptions; 0 items deferred.*

## Questions Asked

*none — see No Ambiguity below*

## No Ambiguity

All behavior in this sub-spec — the endpoint path, JSON fields, three-tier resource URL precedence chain, partial-config error path, and `WWW-Authenticate resource_metadata=` parameter — was established in the operator-confirmed `oauth-prototype-productionize` parent spec (`openspec/changes/oauth-prototype-productionize/specs/oauth-authentication/spec.md`), accepted as part of the [HDX-11442](https://hydrolix.atlassian.net/browse/HDX-11442) OAuth productionization effort. The operator message that established no ambiguity was the approval of that parent spec prior to this sub-spec extraction. This change is a mechanical extraction; no new trade-offs need adjudication here.

## Deferred / Out of Scope

*none*

## Assumptions

*none*
