{
  "manifest_version": "0.4",
  "name": "${DIST_NAME}",
  "display_name": "${DISPLAY_NAME}",
  "version": "${VERSION}",
  "description": "${DESCRIPTION}",
  "long_description": "${LONG_DESCRIPTION}",
  "author": { "name": "${AUTHOR_NAME}", "url": "${AUTHOR_URL}" },
  "license": "Apache-2.0",
  "homepage": "${HOMEPAGE}",
  "repository": { "type": "git", "url": "${REPOSITORY}" },
  "icon": "icon.png",
  "keywords": ${KEYWORDS},
  "server": {
    "type": "uv",
    "entry_point": "src/server.py",
    "mcp_config": {
      "command": "uv",
      "args": ["run", "--directory", "${__dirname}", "src/server.py"],
      "env": {
        "${ENV_PREFIX}URL": "${user_config.${CFG_PREFIX}_url}",
        "${ENV_PREFIX}TOKEN": "${user_config.${CFG_PREFIX}_token}",
        "${ENV_PREFIX}USER": "${user_config.${CFG_PREFIX}_user}",
        "${ENV_PREFIX}PASSWORD": "${user_config.${CFG_PREFIX}_password}",
        "${ENV_PREFIX}DATABASE": "${user_config.${CFG_PREFIX}_database}",
        "${ENV_PREFIX}MCP_SERVER_TRANSPORT": "stdio"
      }
    }
  },
  "tools": [
    { "name": "run_select_query", "description": "Execute a SELECT SQL query on the configured ${DISPLAY_NAME} cluster." },
    { "name": "list_databases",   "description": "List all databases on the ${DISPLAY_NAME} cluster." },
    { "name": "list_tables",      "description": "List all tables in a given database." },
    { "name": "get_table_info",   "description": "Return schema and metadata for a table." }
  ],
  "tools_generated": false,
  "user_config": {
    "${CFG_PREFIX}_url": {
      "type": "string",
      "title": "${DISPLAY_NAME} URL",
      "description": "Canonical URL of your ${DISPLAY_NAME} cluster (e.g. ${EXAMPLE_URL}).",
      "required": true
    },
    "${CFG_PREFIX}_token": {
      "type": "string",
      "title": "Service Account Token",
      "description": "Service-account token. Preferred over username/password. Leave blank if using username/password.",
      "sensitive": true,
      "required": false,
      "default": ""
    },
    "${CFG_PREFIX}_user": {
      "type": "string",
      "title": "Username",
      "description": "${DISPLAY_NAME} username. Used with Password if no Service Account Token is set.",
      "required": false,
      "default": ""
    },
    "${CFG_PREFIX}_password": {
      "type": "string",
      "title": "Password",
      "description": "${DISPLAY_NAME} password. Used with Username if no Service Account Token is set.",
      "sensitive": true,
      "required": false,
      "default": ""
    },
    "${CFG_PREFIX}_database": {
      "type": "string",
      "title": "Default Database",
      "description": "Optional default database. The server uses the cluster default when this is empty.",
      "required": false,
      "default": ""
    }
  },
  "compatibility": {
    "platforms": ["darwin", "win32", "linux"],
    "runtimes": { "python": ">=3.13" }
  }
}
